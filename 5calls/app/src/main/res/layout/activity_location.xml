<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_location"
    style="@style/scrollViewStyle">

    <LinearLayout
        style="@style/mainLinearLayout"
        android:orientation="vertical"
        tools:context="org.a5calls.android.a5calls.controller.LocationActivity">

        <TextView
            android:id="@+id/locationPromptTextView"
            style="@style/headingStyle"
            android:text="@string/location_prompt" />

        <TextView
            android:id="@+id/gpsPromptTextView"
            style="@style/locationGpsTextStyle"
            android:text="@string/location_gps_prompt" />

        <Button
            android:id="@+id/gpsButton"
            style="@style/locationGpsBtnStyle"
            android:text="@string/btn_gps" />

        <TextView
            style="@style/descriptiveTextStyle"
            android:text="@string/location_address_prompt" />

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp">

            <EditText
                android:id="@+id/addressEditText"
                style="@style/zipEditTextStyle" />

            <Button
                android:id="@+id/addressSubmitButton"
                style="@style/submitBtn" />
        </RelativeLayout>

    </LinearLayout>
</ScrollView>
